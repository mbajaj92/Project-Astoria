main

{
	let c<-9;
	let g<-9;
	let m<-c+10;
	let a<-7+b;
	let c<-8*a;
	
	if a<10 then
	
		let b<-b-1;
		
		if b<20 then
			let c<-b+7;
			
		else
			let x<-b-c;
		fi;
		let b<-b-1;
	else
		let m<-m-1;
		if a<a-1 then
			let x<-a+c;
		else
			let x<-a+d;
		fi;
		let f<-f+1;
	fi;

	let g<- e+f;
}.